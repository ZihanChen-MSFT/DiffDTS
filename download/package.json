{
  "name": "zihanc-msft-diff-dts-download",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "deploy": "lerna run deploy --stream"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:ZihancMSFT/DiffDTS.git"
  },
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.16.1"
  }
}